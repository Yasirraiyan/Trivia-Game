def fail(fail):
        if  fail:
            score=score1+score2+score3+score4+score5;
            print(f"You fail.Your score is:{score}")
        if fail and score==0:
            fail==False
             print(f"You Fail.Your score is:{score}")
        
    
